# Palmmi Stage 3K Handoff

## 1. What This Round Did

- Ran 3H, 3I, and 3J baseline tests.
- Added Stage 3K stability test entrypoint.
- Covered failure paths, cache hit/miss/version isolation, schema boundaries, quality gate boundaries, RecognitionResult status flow, mock VLM call counts, cost formulas, and forbidden-path checks.
- Generated Stage 3K and Stage 3 acceptance documentation.

## 2. Added Or Modified Files

Added:

- `tests/stage3/run-stage3k-stability.cjs`
- `docs/stage3/STAGE3K_STABILITY_REPORT.md`
- `docs/stage3/STAGE3_ACCEPTANCE_REPORT.md`
- `docs/stage3/HANDOFF_3K.md`

Modified:

- `docs/stage3/STAGE3_STATE.md`
- `docs/stage3/CHANGE_REQUESTS.md`
- `docs/stage3/STAGE3I_DISTRIBUTION_REPORT.md` was regenerated by the required 3I command.
- `docs/stage3/STAGE3J_PIPELINE_REPORT.md` was regenerated by the required 3J command.

No `app/`, `components/`, `public/`, `package.json`, or `lib/recognition` rule file was modified in Stage 3K.

## 3. Commands Run

```bash
node tests/stage3/run-stage3h-tests.cjs
node tests/stage3/run-stage3i-distribution.cjs
node tests/stage3/run-stage3j-pipeline.cjs
node tests/stage3/run-stage3k-stability.cjs
git status --short -- app components public package.json
```

## 4. Test Results

| Command | Result |
| --- | --- |
| `node tests/stage3/run-stage3h-tests.cjs` | 9/9 passed |
| `node tests/stage3/run-stage3i-distribution.cjs` | 54 samples completed; report regenerated |
| `node tests/stage3/run-stage3j-pipeline.cjs` | 14/14 passed |
| `node tests/stage3/run-stage3k-stability.cjs` | 5/5 passed |

## 5. Cost Statistics Summary

- Stage 3K pipeline executions: 24.
- Mock VLM calls: 17.
- Quality-gate blocked samples before VLM: 6.
- Cache-hit samples: 1.
- Schema-failed samples not entering match: 3.
- Future estimate formula: `estimated_cost = vlm_call_count * cost_per_vlm_call`.
- No-cache estimate: `18 * cost_per_vlm_call`.
- With file_hash cache estimate: `17 * cost_per_vlm_call`.
- Quality/preflight avoided cost estimate: `6 * cost_per_vlm_call`.
- Cache avoided cost estimate: `1 * cost_per_vlm_call`.

## 6. Failure Path Summary

Covered and passed:

- Non-image MIME -> `REJECTED`.
- Non-palm -> `REJECTED`.
- Back of hand -> `REJECTED`.
- Multiple hands -> `REJECTED`.
- Blurry input -> `RETRY_REQUIRED`.
- Too-dark/low-quality score path -> `RETRY_REQUIRED`.
- Low quality but usable -> `LOW_CONFIDENCE`.
- Missing many fields -> `RETRY_REQUIRED`.
- Enum out of range -> `invalid_fields` recorded and degraded.
- Null-heavy input -> `RETRY_REQUIRED`.
- Severe invalid schema -> `RETRY_REQUIRED`.
- Mother core field insufficiency -> `RETRY_REQUIRED`.
- Close Top1/Top2 gap -> `LOW_CONFIDENCE`.

## 7. Cache Test Summary

- Same `file_hash` plus same versions hits cache on the second run.
- Same `file_hash` plus different `rule_version` does not reuse cache.
- Same `file_hash` plus different `schema_version` does not reuse cache.
- Same `file_hash` plus different `prompt_version` does not reuse cache.
- Different `file_hash` does not reuse cache.
- Cache hit is visible in both `cache.cache_hit` and `debug.cache_hit`.
- Cache hit does not reload mock VLM features.

## 8. Forbidden Path Check

- UI not modified: yes.
- `app/`, `components/`, `public/`, `package.json` not modified: yes.
- API not connected: yes.
- Real VLM not called: yes.
- Check command output for forbidden paths was empty.

## 9. Known Risks

Stage 3I risks are retained, not fixed in 3K:

- M7 mother type 2/54, below 5%, WARNING.
- P27, P26, P29, P04, P32, P24 have zero final hits.
- Top1/Top2 gap `< 0.15` appears in 33/54 samples.
- P05/P07 and P25/P33 have zero final adjacent triggers.
- Cross-mother correction is 3/54 and not overactive.

These should move to later real-sample calibration, not Stage 3K rule tuning.

## 10. Can 3K Close

Yes. Stage 3K regression and stability acceptance passed without UI changes, API integration, real VLM calls, or rule changes.

## 11. Next Step

Generate final Stage 3 acceptance and Stage 4 handoff materials, then hand the stable `RecognitionResult` interface to page productization. Real VLM integration should remain a later dedicated stage.
